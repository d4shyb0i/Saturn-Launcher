<Window x:Class="SaturnLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Saturn Client" Height="600" Width="1000" 
        WindowStartupLocation="CenterScreen" Background="Transparent"
        WindowStyle="None" AllowsTransparency="True"
        ContentRendered="Window_ContentRendered">

    <Border CornerRadius="20" Background="#121212" BorderBrush="#333" BorderThickness="1">
        <Grid>
            <Canvas x:Name="StarCanvas" IsHitTestVisible="False" Opacity="0.5"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="#CC181818" CornerRadius="20,0,0,20" MouseLeftButtonDown="DragWindow">
                    <StackPanel Margin="20">
                        <StackPanel x:Name="ProfileSection" Orientation="Horizontal" Margin="0,0,0,30" Background="Transparent" Cursor="Hand" MouseLeftButtonDown="ProfileSection_MouseLeftButtonDown">
                            <StackPanel.ContextMenu>
                                <ContextMenu Background="#222">
                                    <MenuItem Header="LOGOUT" Click="Logout_Click" Foreground="Red" FontWeight="Bold"/>
                                </ContextMenu>
                            </StackPanel.ContextMenu>
                            <Border Width="40" Height="40" CornerRadius="5" Background="#252525">
                                <Image x:Name="imgPlayerHead" Stretch="Fill" RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center" Margin="10,0,0,0">
                                <TextBlock x:Name="txtPlayerName" Text="Guest" Foreground="White" FontWeight="Bold" FontSize="14"/>
                                <TextBlock x:Name="lblOnlineStatus" Text="Offline" Foreground="Gray" FontSize="10"/>
                            </StackPanel>
                        </StackPanel>
                        <Button Content="DASHBOARD" Height="40" Background="Transparent" Foreground="White" BorderThickness="0" HorizontalContentAlignment="Left"/>
                        <Separator Background="#333" Margin="0,20"/>
                        <Button x:Name="btnLogin" Content="LOG IN" Height="35" Background="#252525" Foreground="White" Click="btnLogin_Click"/>
                    </StackPanel>
                </Border>

                <Grid Grid.Column="1">
                    <Grid x:Name="MainDashboard" Margin="20">
                        <TextBlock Text="SATURN" FontSize="80" FontWeight="Black" Foreground="Cyan" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,100">
                            <TextBlock.Effect>
                                <DropShadowEffect BlurRadius="15" Color="Cyan" ShadowDepth="0"/>
                            </TextBlock.Effect>
                        </TextBlock>
                        <StackPanel VerticalAlignment="Bottom" Margin="0,0,0,20">
                            <TextBlock x:Name="lblStatus" Text="READY" Foreground="Gray" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <ProgressBar x:Name="pbDownload" Height="6" Background="#222" Foreground="Cyan" BorderThickness="0" Margin="60,0,60,15" Visibility="Collapsed"/>
                            <Button x:Name="btnLaunch" Content="PLAY" Width="320" Height="65" Background="Cyan" Foreground="Black" FontWeight="Bold" FontSize="22" Click="btnLaunch_Click"/>
                        </StackPanel>
                    </Grid>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10">
                        <Button Content="—" Background="Transparent" Foreground="Gray" Width="30" Click="MinimizeButton_Click"/>
                        <Button Content="✕" Background="Transparent" Foreground="Gray" Width="30" Click="CloseButton_Click"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>